<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="000-migrate-existing-users" author="migration">
        <preConditions onFail="MARK_RAN">
            <and>
                <tableExists tableName="users"/>
                <not>
                    <columnExists tableName="users" columnName="status"/>
                </not>
            </and>
        </preConditions>
        <comment>Add missing status and updated_at columns to existing users table</comment>
        <addColumn tableName="users">
            <column name="status" type="VARCHAR(20)" defaultValue="ACTIVE">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <sql>
            <![CDATA[
            -- Update existing rows to have status = ACTIVE if not set
            UPDATE users SET status = 'ACTIVE' WHERE status IS NULL;
            UPDATE users SET updated_at = created_at WHERE updated_at IS NULL;
            ]]>
        </sql>
    </changeSet>

</databaseChangeLog>

