package com.onelpro.librelog.enums;

/**
 * Represents the possible approval statuses for orders and campaigns.
 */
public enum ApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED,
    NOT_REQUIRED
}

